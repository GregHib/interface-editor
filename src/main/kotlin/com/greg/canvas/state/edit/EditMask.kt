package com.greg.canvas.state.edit

import javafx.scene.Group
import javafx.scene.Scene
import javafx.scene.paint.Color


class EditMask {

    private val group = Group()
    var scene = Scene(group, 765.0, 503.0, Color.ALICEBLUE)


}